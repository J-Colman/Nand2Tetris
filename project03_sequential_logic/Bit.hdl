/*  1-bit Register:
    If load(t-1) == 1 then out(t) = in(t-1)
    Else out(t) = out(t-1). */
CHIP Bit {
    IN in, load;
    OUT out;
    PARTS:
    Mux(a=prevout, b=in, sel=load, out=next);
    DFF(in=next, out=prevout, out=out);
}