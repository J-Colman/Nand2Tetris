/*	Xor gate:
	out=1 only when a=1 OR b=1 exclusively, else out=0. */
CHIP Xor {
	IN a, b;
	OUT out;
	PARTS:
    Nand(a=a, b=b, out=w1);
	Nand(a=a, b=w1, out=w2);
	Nand(a=b, b=w1, out=w3);
    Nand(a=w2, b=w3, out=out);
}
